# Backsnap Config File

# ------------------------------------------------------------------------
# Fast Net:  If you are backing up over a LAN, set this to true, and it
# will optimize for that.  If you are backing up over a slow link, such
# as across the Internet, set it to false
FASTNET=true
